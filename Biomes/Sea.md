water routes between [[Tempered Forest]] and [[Tropical]]

```dataviewjs
const { Pokemon } = await cJS();
dv.el("div").replaceChildren(
	Pokemon.Render.SpriteList(
		Pokemon.LinkedIn(dv, [
			"Water Route 1",
			"Water Route 2"
		])
	)
);
```
